""" Django app configuration for the results app. """
from django.apps import AppConfig


class ResultsConfig(AppConfig):
    """ Django app configuration for the results app. """
    default_auto_field = "django.db.models.BigAutoField"
    name = "results"
